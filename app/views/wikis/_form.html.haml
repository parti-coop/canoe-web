= form_for (@wiki.new_record? ? [@canoe, @wiki] : @wiki) do |f|
  .form-group
    = f.label :title
    = f.text_field :title, class: 'form-control'
  .form-group
    = f.label :body
    ~ f.text_area :body, rows: 15, class: 'form-control action-autoresize'
  .form-group
    = f.submit '저장', class: 'btn btn-primary'
