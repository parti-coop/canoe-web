= form_for (@category.new_record? ? [@canoe, @category] : @category) do |f|
  .form-group
    = f.label :name
    = f.text_field :name, class: 'form-control'
  .form-group
    = f.submit class: 'btn btn-primary'
