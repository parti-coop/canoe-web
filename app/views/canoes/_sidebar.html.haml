%ul.nav.nav-sidebar
  %li
    = link_to @canoe do
      = image_tag @canoe.logo_url, style: 'width: 100%; height: 150px;'

    - if @canoe.receivable_boarding_request? current_user
      %div{ style: 'padding: 0 20px;' }
        = link_to '가입 신청', boarding_requests_path(canoe_id: @canoe), method: :post, class: 'btn btn-primary btn-block'
    - if @canoe.boarding_requested? current_user
      %div{ style: 'padding: 0 20px;' }
        가입 신청 중

%ul.nav.nav-sidebar
  %li
    = link_to 'Discussions', canoe_discussions_path(@canoe)
  %li
    %a{:href => "#"} Votes
  %li
    = link_to 'Sailing Diaries', canoe_sailing_diaries_path(@canoe)
  %li
    %a{:href => "#"} Wiki
  %li
    %a{:href => "#"} Chat
  %li
    = link_to canoe_memberships_path(@canoe) do
      Members
      %span.count= @canoe.memberships.count
